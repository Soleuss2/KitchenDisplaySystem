# Render Configuration File
# This file helps Render auto-configure your service
# Place this in the root of your repository

services:
  - type: web
    name: kitchen-system-kiosk
    env: docker
    dockerfilePath: ./Dockerfile
    # Dockerfile method is recommended - includes .NET SDK automatically
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: DataCon__ConnectionString
        sync: false  # Set this manually in Render dashboard for security
    # Alternative: Native .NET build (if Docker doesn't work)
    # env: docker  # Sometimes works better than 'dotnet'
    # buildCommand: |
    #   curl -sSL https://dot.net/v1/dotnet-install.sh | bash /dev/stdin --channel 8.0
    #   export PATH="$PATH:$HOME/.dotnet"
    #   dotnet restore
    #   dotnet publish -c Release -o ./publish
    # startCommand: |
    #   export PATH="$PATH:$HOME/.dotnet"
    #   dotnet ./publish/SelfOrderingSystemKiosk.dll

